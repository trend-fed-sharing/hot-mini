# mini-dev
Minimum webpack / babel environment. Only for demo or workshop purposes.

Both `dev` and `start` will work.

## yarn

```sh
yarn
yarn dev
# or yarn start
```

## npm

```sh
npm install
npm dev
# or npm start
```
